# Arquivo de exemplo de variáveis de ambiente para o Frame Agent CLI
# Copie este arquivo para .env e configure suas variáveis

# Chave API do OpenAI (obrigatório para providers OpenAI)
OPENAI_API_KEY=sua_chave_api_aqui

# Modelo a ser usado (opcional, padrão: gpt-4o-mini)
MODEL=Qwen/Qwen3-Coder-480B-A35B-Instruct-FP8

# URL base para providers OpenAI (opcional, para providers customizados)
OPENAI_BASE_URL=https://api.openai.com/v1

# Provider a ser usado (opcional, padrão: openai-generic)
# Opções: openai-gpt-4o, openai-gpt-4o-mini, openai-generic
PROVIDER=openai-generic

# Ativar modo de debug (opcional, padrão: true)
# Quando true, mostra logs detalhados do processo ReAct
# Quando false, desativa todos os logs
DEBUG=true
ENABLE_LOGGING=true

# Temperatura para geração de texto (opcional, padrão: 0.7)
TEMPERATURE=0.7

# Número máximo de tokens na resposta (opcional, padrão: 1000)
MAX_TOKENS=1000

# Instruções do sistema personalizadas (opcional)
INSTRUCTIONS="Você é um assistente especializado em desenvolvimento de software."